export const matrixGenerator=(count)=>{

    // Map creation
    const PlayerMap = new Map();

    // Columns  = 9
    const columns = 9;

    // Count = 15 Numbers 
    const items = 15;

    let activities = [];
        
    for(var i=0;i<count;i++)
    {
        activities.push([])
        
    }

console.log(count);
}